 {
  "name": "mynode",
  "version": "1.0.0",
  "description": "",
  "main": "dist/mynode.umd.js",
  "module": "dist/mynode.mjs",
  "files": [
    "dist/",
    "public/"
  ],
  "scripts": {
    "build-wasm": "cd rust-wasm && wasm-pack build --target web",
    "build": "vite build && npm run build-demo",
    "build-demo": "cd demo && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vite": "^5.4.10",
    "vite-plugin-top-level-await": "^1.4.4",
    "vite-plugin-wasm": "^3.3.0"
  }
}
